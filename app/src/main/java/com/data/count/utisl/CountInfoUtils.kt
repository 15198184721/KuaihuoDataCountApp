package com.data.count.utisl

import com.data.count.bean.TestABean

/**
 * 统计信息的工具类，主要是提供一些通用节点的信息类型
 */
object CountInfoUtils {

    /**
     * 用户登录成功的统计
     * @param info UserInfo
     */
    fun loginSuccessCount(info: TestABean){
    }

    /**
     * 用户退出登录的统计
     */
    fun loginOutCount(){}
}